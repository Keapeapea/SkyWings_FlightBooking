<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Flight - SkyWings Admin</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container">
        <%- include('partials/flash') %>

        <a href="/admin" style="display:inline-block; margin-bottom:16px; font-weight:600;">&larr; Back to flights</a>

        <div class="card">
            <h2>Add New Flight</h2>
            <form action="/admin/flights/add" method="POST">
                <div class="field">
                    <label for="flight_number">Flight Number</label>
                    <input type="text" id="flight_number" name="flight_number" placeholder="e.g. SQ118" required>
                </div>
                <div class="field">
                    <label for="airline">Airline</label>
                    <input type="text" id="airline" name="airline" value="SkyWings Airlines" required>
                </div>
                <div class="field">
                    <label for="origin">Origin</label>
                    <input type="text" id="origin" name="origin" required>
                </div>
                <div class="field">
                    <label for="destination">Destination</label>
                    <input type="text" id="destination" name="destination" required>
                </div>
                <div class="field">
                    <label for="departure_date">Departure Date</label>
                    <input type="date" id="departure_date" name="departure_date" required>
                </div>
                <div class="field">
                    <label for="departure_time">Departure Time</label>
                    <input type="time" id="departure_time" name="departure_time" required>
                </div>
                <div class="field">
                    <label for="arrival_time">Arrival Time</label>
                    <input type="time" id="arrival_time" name="arrival_time" required>
                </div>
                <div class="field">
                    <label for="price">Price (economy, $)</label>
                    <input type="number" id="price" name="price" step="0.01" min="50" required>
                </div>
                <div class="field">
                    <label for="total_seats">Total Seats</label>
                    <input type="number" id="total_seats" name="total_seats" min="1" required>
                </div>
                <button type="submit" class="btn btn-accent">Add Flight</button>
            </form>
        </div>
    </div>

    <script>
        document.getElementById('departure_date').min = new Date().toISOString().split('T')[0];

        document.querySelector('form').addEventListener('submit', function(e) {
            const dep = document.getElementById('departure_time').value;
            const arr = document.getElementById('arrival_time').value;
            if (arr <= dep) {
                alert('Arrival time must be after departure time.');
                e.preventDefault();
            }
        });
    </script>

</body>
</html>
